#include <civic.h>

export int printIntVec(int[n] vec) {
    for (int i = 0, i < n, i = i + 1) {
        printInt(vec[i]);
    }
}

export int printFloatVec(float[n] vec) {
    for (int i = 0, i < n, i = i + 1) {
        printFloat(vec[i]);
    }
}


